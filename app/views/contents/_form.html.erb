<%= simple_form_for([@collection.becomes(Collection), @content]) do |f| %>

    <%= f.input :title %>
    <%= f.input :body %>
    <%= f.input :location %>

    <%= f.button :submit, :class => 'btn btn-primary' %>
<% end %>
