<%= simple_form_for([:settings, @profile], url: settings_profile_path) do |f| %>

    <%= f.input :first_name %>
    <%= f.input :last_name %>
    <%= f.input :gravatar_email %>

    <%= f.button :submit, :class => 'btn btn-primary' %>
<% end %>
