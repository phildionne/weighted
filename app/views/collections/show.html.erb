<%= render @collection %>

<%= link_to 'Edit', edit_collection_path(@collection) %> |
<%= link_to 'Back', collections_path %>

<hr>

<%= render 'follow_form', collection: @collection %>

<hr>

<h3>Followers</h3>

<ol>
  <%= render partial: 'users/user_listing', collection: @collection.followers, as: :user %>
</ol>

<hr>

<h3>Sources</h3>

<%= render partial: 'sources/source_listing', collection: @collection.sources, as: :source %>

<hr>

<h3>Contents</h3>

<%= render partial: 'contents/content_listing', collection: @collection.contents, as: :content %>
